export class NewTask {
  constructor(title, description, dueDate, bgColor, label, index) {
    this.title = title;
    this.description = description;
    this.dueDate = dueDate;
    this.bgColor = bgColor;
    this.label = label;
    this.index = index;
  }
}
